import ClientPage from './ClientPage';

export const metadata = {
  title: 'Convert Word and TXT to PDF Online - Free & Secure | LocalPDF',
  description: 'Convert Microsoft Word (.docx) and plain text (.txt) files into high-quality PDFs 100% locally in your browser. Maximum privacy, no file uploads.',
};

export default function Page() {
  return <ClientPage />;
}
